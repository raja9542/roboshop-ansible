- name: Copy MongoDB Yum Repo file
  ansible.builtin.copy:
    src: mongodb.repo
    dest: /etc/yum.repos.d/mongodb.repo

- name: Install MongoDB Server
  ansible.builtin.yum:
    name: mongodb-org-shell
    state: installed

#- name: Load Schema
#  ansible.builtin.shell: mongo --host mongodb-dev.devopsraja66.online < /app/schema/{{component}}.js

- name: Load Schema
  ansible.builtin.shell: mongo --ssl --host dev-docdb-cluster.cluster-cpy5ddqj3otr.us-east-1.docdb.amazonaws.com:27017 --sslCAFile /app/rds-combined-ca-bundle.pem --username {{ lookup('aws_ssm', "{{env}}.docdb.DB_ADMIN_USER", region='us-east-1' ) }} --password {{ lookup('aws_ssm', "{{env}}.docdb.DB_ADMIN_PASS", region='us-east-1' ) }}

# we need to copy the code from amazon document db and change the location to /app/rds-combined-ca-bundle.pem b/c we dowloaded the CA in that location
# we need to provide docdb username and password